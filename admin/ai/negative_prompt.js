// admin/ai/negative_prompt.js
export async function viewAiNegativePrompt(current = null) {
  const exists = current && String(current).trim().length > 0;
  return [
    "🚫 <b>Негатив промпт</b>",
    "",
    "Что бот <u>не должен</u> обсуждать или должен отвечать осуждающе — перечислите <u>через запятую</u>.",
    "Пример: <i>политика, расизм, оскорбления, угрозы, слив личных данных</i>",
    "",
    "⚙️ <b>Как работает:</b> если в сообщении зрителя встречается что-то из списка — бот <b>не отвечает по сути</b> и даёт короткое осуждение.",
    "",
    exists ? `Текущий негатив промпт:\n<code>${current}</code>` : "Текущий негатив промпт: <i>не задан</i>",
    "",
    "Отправьте список сообщением в чат (через запятую).",
  ].join("\n");
}
