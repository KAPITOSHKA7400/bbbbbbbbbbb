// admin/ai/prompt.js
export async function viewAiPrompt(current = null) {
  const exists = current && String(current).trim().length > 0;
  return [
    "📝 <b>Промпт</b>",
    "",
    "Добавьте факты о себе/стриме/игре <u>через запятую</u>.",
    "Пример: <i>стример из СПб, играю в Escape From Tarkov, люблю снайперку, семейный контент</i>",
    "",
    "⚙️ <b>Как работает:</b> промпт учитывается в ответах <b>только если</b> в сообщении зрителя есть упоминание чего-то из вашего списка.",
    "",
    exists ? `Текущий промпт:\n<code>${current}</code>` : "Текущий промпт: <i>не задан</i>",
    "",
    "Отправьте ваш список сообщением в чат (через запятую).",
  ].join("\n");
}
